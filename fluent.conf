<source>
  @type  forward
  @label @default
  port  24224
  bind 0.0.0.0
</source>

<label @FLUENT_LOG>
  <match fluent.*>
    @type stdout
  </match>
</label>

<label @default>
  <match **>
    @type stdout
  </match>
</label>
